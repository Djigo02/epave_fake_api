{
  "name": "back_epave",
  "version": "1.0.0",
  "private": true,
  "description": "API mock epave via json-server",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
